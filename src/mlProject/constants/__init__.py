from pathlib import Path
#<---recordar que resolvimos el conflicto del forward slash para automatizar mejor el proceso.
#<---por lo que de ahora en adelante, la busqueda de rutas/path no necesitará de cambios manuales del backslash a forward slash.
CONFIG_FILE_PATH = Path("config/config.yaml")
PARAMS_FILE_PATH = Path("params.yaml")
SCHEMA_FILE_PATH = Path("schema.yaml")